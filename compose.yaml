version: '3'

services:
  gamegenrepopularity-airflow:
    image: gamegenrepopularity-airflow:latest
    volumes:
      - ./airflow:/opt/airflow
    
    ports:
      - "8080:8080"
    
    command: airflow standalone